name: Test and Build Docker

on:
  push:
  pull_request:

jobs:
  docker-build-and-test:
    name: Build and Test Docker Image
    runs-on: ubuntu-latest

    services:
      postgres:
        image: postgres:15
        env:
          POSTGRES_USER: test
          POSTGRES_PASSWORD: test
          POSTGRES_DB: testdb
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5
        ports:
          - 5432:5432

    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Cache Docker layers
        uses: actions/cache@v4
        with:
          path: /tmp/.buildx-cache
          key: ${{ runner.os }}-buildx-${{ hashFiles('dockerfile', 'package*.json') }}
          restore-keys: |
            ${{ runner.os }}-buildx-

      - name: Build Docker image
        uses: docker/build-push-action@v5
        with:
          context: .
          push: false
          load: true
          tags: be-repo:latest
          cache-from: type=local,src=/tmp/.buildx-cache
          cache-to: type=local,dest=/tmp/.buildx-cache-new,mode=max

      - name: Move cache
        run: |
          rm -rf /tmp/.buildx-cache
          mv /tmp/.buildx-cache-new /tmp/.buildx-cache

      - name: Run Docker container
        run: |
          docker run -d --name test-container -p 4000:4000 \
            --add-host=host.docker.internal:host-gateway \
            -e DATABASE_URL="postgresql://test:test@host.docker.internal:5432/testdb" \
            -e JWT_SECRET="test-secret-key-for-ci" \
            -e JWT_EXPIRES_IN="7d" \
            -e FRONTEND_URL="http://localhost:5000" \
            -e ALLOWED_CORS_ORIGINS="http://localhost:5000" \
            -e NODE_ENV="production" \
            -e PORT="4000" \
            be-repo:latest

      - name: Wait for application to be ready
        run: |
          echo "Waiting for application to be ready..."
          for i in {1..30}; do
            if curl -s http://localhost:4000/api/docs > /dev/null 2>&1; then
              echo "✓ Application is ready after ${i} seconds"
              break
            fi
            if [ $i -eq 30 ]; then
              echo "✗ Application failed to start within 30 seconds"
              echo "Container logs:"
              docker logs test-container
              exit 1
            fi
            sleep 1
          done

      - name: Test application endpoint
        run: |
          RESPONSE=$(curl -s -o /dev/null -w "%{http_code}" http://localhost:4000/api/docs)
          echo "HTTP Status: $RESPONSE"
          if [ "$RESPONSE" = "200" ] || [ "$RESPONSE" = "301" ]; then
            echo "✓ Test passed: Swagger docs accessible"
            exit 0
          else
            echo "✗ Test failed: Got HTTP $RESPONSE"
            docker logs test-container
            exit 1
          fi

      - name: Stop and remove container
        if: always()
        run: |
          docker stop test-container || true
          docker rm test-container || true
